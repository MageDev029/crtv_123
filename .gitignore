# Model weights — downloaded by CI or by download_weights.sh on host.
# Excluded from git to keep repo small; included in the Docker build context.
models/

# Experiment runner — not used by production worker, keep local-only.
latentsync_runner.py

# Strategy doc — don't publish what we learned to a repo competitors can mirror.
discussion.md

# Local artifacts
.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
*.log
